<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <permission
        android:name="com.exemple.floracare.permission.READ_PLANTS"
        android:protectionLevel="dangerous"
        android:label="Read FloraCare Plants"
        android:description="Allows reading plant data from FloraCare" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.FloraCare"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.auth.RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".ui.list.PlantListActivity"
            android:exported="false" />

        <activity
            android:name=".ui.detail.PlantDetailActivity"
            android:exported="false" />

        <provider
            android:name=".provider.PlantContentProvider"
            android:authorities="com.exemple.floracare.provider"
            android:exported="true"
            android:readPermission="com.exemple.floracare.permission.READ_PLANTS" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.exemple.floracare.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
